

const Table = ({list, colNames, width='auto', height='auto'}) => {
    return (
    <div>
        {list?.length > 0 && (
            <table>
                
            </table>
        )}
    </div>
    )
}

export default Table
